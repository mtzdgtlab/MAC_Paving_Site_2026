---
// HeroBanner.astro - Optimizado para LCP
---

<script is:inline>
  // Agregamos esto para el FCP - Efecto de carga rápida
  document.addEventListener("DOMContentLoaded", () => {
    // Marcar la imagen principal como LCP (Largest Contentful Paint)
    const heroImage = document.querySelector(".hero-banner img");
    if (heroImage) {
      heroImage.setAttribute("fetchpriority", "high");

      // Registro de tiempo LCP para análisis
      new PerformanceObserver((entryList) => {
        for (const entry of entryList.getEntries()) {
          console.log("LCP:", entry.startTime, entry);
        }
      }).observe({ type: "largest-contentful-paint", buffered: true });
    }
  });
</script>

<style is:inline>
  /* Estilos críticos pre-cargados para el banner - mejora FCP */
  .hero-banner {
    position: relative;
    background: #f8f9fa; /* Placeholder color mientras carga la imagen */
    min-height: 400px;
    width: 100%;
    display: flex;
    align-items: center;
    overflow: hidden;
    padding-top: 120px;
    padding-bottom: 120px;
  }

  .hero-banner__content {
    position: relative;
    z-index: 2;
    max-width: 650px;
  }

  .hero-banner__title {
    font-size: 3rem;
    line-height: 1.2;
    margin-bottom: 1.5rem;
    color: #fff;
    font-weight: 700;
  }

  .hero-banner__text {
    font-size: 1.125rem;
    line-height: 1.6;
    margin-bottom: 2rem;
    color: #fff;
  }

  .hero-banner__btn {
    display: inline-flex;
    background: #1a3164;
    color: #fff;
    padding: 0.8rem 1.5rem;
    border-radius: 4px;
    font-weight: 600;
    text-decoration: none;
    transition: all 0.3s ease;
  }

  .hero-banner__btn:hover {
    background: #007bff;
    transform: translateY(-3px);
  }

  /* Estilos para dispositivos móviles */
  @media (max-width: 767px) {
    .hero-banner__title {
      font-size: 2rem;
    }
  }
</style>

<!-- Esta es una directiva que se puede usar en lugar del componente Banner existente -->
