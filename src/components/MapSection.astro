---
import MapComponent from "@/components/MapComponent.astro";
---

<section
  aria-label="Service Areas Full Map"
  class="project-section section-space fix"
>
  <div class="small-container">
    <div
      class="small-container"
      style="max-width: 900px; margin: 0 auto; text-align: center;"
    >
      <span class="section-sub-title">North Jersey Coverage</span>
      <h2 class="section-title mt-35 mb-35">Service Areas</h2>
      <p style="margin-bottom: 30px;">
        MAC Paving and Sealcoating LLC dispatches owner-led crews across
        <strong
          >Passaic, Bergen, Morris, Essex, Hudson, Union, Middlesex, Somerset,
          and Sussex</strong
        >
        counties for driveway paving, sealcoating, resurfacing, extensions, and
        paver projects.
      </p>
    </div>
    <!-- Full-width map iframe -->
    <div
      class="map-container"
      style="width: 100%; height: 620px; overflow: hidden; position: relative; z-index: 1;"
    >
      <MapComponent />
    </div>
  </div>
</section>

<style>
  /* Asegurar que el mapa y todos sus elementos tengan z-index bajo */
  .map-container {
    position: relative;
    z-index: 1 !important;
  }

  .map-container * {
    z-index: 1 !important;
  }

  /* Específico para Leaflet */
  .map-container .leaflet-container,
  .map-container .leaflet-map-pane,
  .map-container .leaflet-control-container {
    z-index: 1 !important;
  }

  /* Asegurar que los controles del mapa no interfieran */
  .map-container .leaflet-control {
    z-index: 10 !important; /* Solo para controles del mapa */
  }
</style>
